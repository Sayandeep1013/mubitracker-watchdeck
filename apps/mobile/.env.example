EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Local dev: http://localhost:3000 with `adb reverse tcp:3000 tcp:3000` (Android) so the
# device's "localhost" reaches the dev machine. Otherwise point at the deployed API URL.
EXPO_PUBLIC_API_URL=http://localhost:3000
